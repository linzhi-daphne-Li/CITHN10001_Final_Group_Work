01F00113    # addi x2, x0, 31       ; x2=31，排序总轮数
00000193    # addi x3, x0, 0        ; i=0，外层循环计数器

06218063    # beq  x3, x2, done     ; i==31时排序结束
40310233    # sub  x4, x2, x3       ; 当前轮比较次数=31-i
00000293    # addi x5, x0, 0        ; j=0，内层循环计数器
00001337    # lui  x6, 0x1          ; x6=0x1000，数组首地址
00000013    # nop                   ; 流水线等待
00000013    # nop                   ; 流水线等待

00032383    # lw   x7, 0(x6)        ; x7=array[j]
00432403    # lw   x8, 4(x6)        ; x8=array[j+1]
00000013    # nop                   ; 等待内存读取
00000013    # nop                   ; 等待内存读取

007424B3    # slt  x9, x8, x7       ; x9=(array[j+1]<array[j])
00000013    # nop                   ; 等待比较结果
00048863    # beq  x9, x0, no_swap  ; x9=0则不交换

00832023    # sw   x8, 0(x6)        ; array[j]=原array[j+1]
00732223    # sw   x7, 4(x6)        ; array[j+1]=原array[j]
00000013    # nop                   ; 等待写入完成

00430313    # addi x6, x6, 4        ; 指针移动到下一元素
00000013    # nop                   ; 流水线等待
00000013    # nop                   ; 流水线等待
00128293    # addi x5, x5, 1        ; j++
00428463    # beq  x5, x4, next_i   ; 当前轮比较完成
FC5FF06F    # jal  x0, inner_loop   ; 继续内层循环

00118193    # addi x3, x3, 1        ; i++
FA5FF06F    # jal  x0, outer_loop   ; 开始下一轮

00002537    # lui  x10, 0x2         ; x10=0x2000，状态地址
CAFEC5B7    # lui  x11, 0xCAFEC     ; x11=0xCAFEC000
ABE58593    # addi x11, x11, -1346  ; x11=0xCAFEBABE
00B52023    # sw   x11, 0(x10)      ; [0x2000]=0xCAFEBABE
0000006F    # jal  x0, 0            ; 原地无限循环，程序停止